financeex:
  storage:
    provider: s3
    s3:
      bucket: ${FINANCEEX_S3_BUCKET:financeex-documents}
      region: ${FINANCEEX_S3_REGION:us-east-1}
      endpoint: ${FINANCEEX_S3_ENDPOINT:http://localhost:9000}
      access-key: ${FINANCEEX_S3_ACCESS_KEY:fin_supervisor}
      secret-key: ${FINANCEEX_S3_SECRET_KEY:kunone123}
      path-style-access-enabled: ${FINANCEEX_S3_PATH_STYLE_ACCESS_ENABLED:true}
      key-prefix: ${FINANCEEX_S3_KEY_PREFIX:documents}
