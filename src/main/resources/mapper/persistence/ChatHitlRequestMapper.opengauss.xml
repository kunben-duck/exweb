<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huawei.finance.front.one.infrastructure.persistence.ChatHitlRequestMapper">
    <resultMap id="chatHitlRequestResultMap" type="com.huawei.finance.front.one.infrastructure.persistence.ChatHitlRequestRow">
        <result column="tenant_id" property="tenantId"/>
        <result column="user_id" property="userId"/>
        <result column="session_id" property="sessionId"/>
        <result column="source_run_id" property="sourceRunId"/>
        <result column="continue_run_id" property="continueRunId"/>
        <result column="user_message_id" property="userMessageId"/>
        <result column="assistant_message_id" property="assistantMessageId"/>
        <result column="runtime_provider" property="runtimeProvider"/>
        <result column="runtime_binding_id" property="runtimeBindingId"/>
        <result column="runtime_session_id" property="runtimeSessionId"/>
        <result column="approval_id" property="approvalId"/>
        <result column="waiting_type" property="waitingType"/>
        <result column="status" property="status"/>
        <result column="request_payload_json" property="requestPayloadJson"/>
        <result column="response_payload_json" property="responsePayloadJson"/>
        <result column="expires_at" property="expiresAt"/>
        <result column="answered_at" property="answeredAt"/>
        <result column="cancelled_at" property="cancelledAt"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="hitlColumns">
        id, tenant_id, user_id, session_id, source_run_id, continue_run_id, user_message_id,
        assistant_message_id, runtime_provider, runtime_binding_id, runtime_session_id, approval_id, waiting_type,
        status, request_payload_json, response_payload_json, expires_at, answered_at, cancelled_at,
        created_at, updated_at
    </sql>

    <!--
      创建协议级等待用户输入请求，供后续澄清/审批结果续接 Runtime。
      入参: ChatHitlRequestRow，id 为 HITL 主键，tenantId/userId/sessionId 为归属边界，sourceRunId/
            userMessageId/assistantMessageId 关联原问答，runtimeProvider/runtimeBindingId/runtimeSessionId/approvalId 用于续接，
            requestPayloadJson 保存 Relay 原始请求，expiresAt 为可提交截止时间。
    -->
    <insert id="insert" parameterType="com.huawei.finance.front.one.infrastructure.persistence.ChatHitlRequestRow">
        INSERT INTO fin_ex_chat_hitl_request_t(
            id, tenant_id, user_id, session_id, source_run_id, continue_run_id, user_message_id,
            assistant_message_id, runtime_provider, runtime_binding_id, runtime_session_id, approval_id, waiting_type,
            status, request_payload_json, response_payload_json, expires_at, answered_at, cancelled_at,
            created_at, updated_at
        )
        VALUES (
            #{id}, #{tenantId}, #{userId}, #{sessionId}, #{sourceRunId}, #{continueRunId}, #{userMessageId},
            #{assistantMessageId}, #{runtimeProvider}, #{runtimeBindingId}, #{runtimeSessionId}, #{approvalId}, #{waitingType},
            #{status}, #{requestPayloadJson}, #{responsePayloadJson}, #{expiresAt}, #{answeredAt}, #{cancelledAt},
            #{createdAt}, #{updatedAt}
        )
    </insert>

    <!--
      按 owner 和 HITL ID 查询请求，供提交响应和排障使用。
      入参: tenantId/userId 为归属边界，hitlRequestId 为目标 HITL 请求。
    -->
    <select id="findByOwnerAndId" resultMap="chatHitlRequestResultMap">
        SELECT <include refid="hitlColumns"/>
        FROM fin_ex_chat_hitl_request_t
        WHERE tenant_id = #{tenantId}
          AND user_id = #{userId}
          AND id = #{hitlRequestId}
    </select>

    <!--
      查询会话当前仍等待用户输入的请求，stream-status 用它生成等待摘要。
      入参: tenantId/userId/sessionId 为归属边界。
    -->
    <select id="findWaitingBySession" resultMap="chatHitlRequestResultMap">
        SELECT <include refid="hitlColumns"/>
        FROM fin_ex_chat_hitl_request_t
        WHERE tenant_id = #{tenantId}
          AND user_id = #{userId}
          AND session_id = #{sessionId}
          AND status = 'WAITING'
        ORDER BY created_at DESC
        LIMIT 1
    </select>

    <!--
      原子声明用户响应，防止多个页签对同一个澄清请求重复提交。
      入参: ChatHitlClaimRow，tenantId/userId/hitlRequestId 为归属和目标，continueRunId 为续接 run，
            responsePayloadJson 为用户回答，now 为状态更新时间。
    -->
    <update id="claimForResponse" parameterType="com.huawei.finance.front.one.infrastructure.persistence.ChatHitlClaimRow">
        UPDATE fin_ex_chat_hitl_request_t
        SET status = 'RESPONDING',
            continue_run_id = #{continueRunId},
            response_payload_json = #{responsePayloadJson},
            updated_at = #{now}
        WHERE tenant_id = #{tenantId}
          AND user_id = #{userId}
          AND id = #{hitlRequestId}
          AND status = 'WAITING'
          AND (expires_at IS NULL OR expires_at &gt; #{now})
    </update>

    <!--
      续接 run 正常完成后标记 HITL 已回答。
      入参: tenantId/userId 为归属边界，hitlRequestId 为目标请求，answeredAt 为完成时间。
    -->
    <update id="markAnswered">
        UPDATE fin_ex_chat_hitl_request_t
        SET status = 'ANSWERED',
            answered_at = #{answeredAt},
            updated_at = #{answeredAt}
        WHERE tenant_id = #{tenantId}
          AND user_id = #{userId}
          AND id = #{hitlRequestId}
          AND status = 'RESPONDING'
    </update>

    <!--
      续接失败时退回等待态，允许用户重试提交同一澄清请求。
      入参: tenantId/userId 为归属边界，hitlRequestId 为目标请求。
    -->
    <update id="markWaiting">
        UPDATE fin_ex_chat_hitl_request_t
        SET status = 'WAITING',
            continue_run_id = NULL,
            updated_at = CURRENT_TIMESTAMP
        WHERE tenant_id = #{tenantId}
          AND user_id = #{userId}
          AND id = #{hitlRequestId}
          AND status = 'RESPONDING'
    </update>

    <!--
      会话删除、stop 或管理动作取消开放的等待请求。
      入参: tenantId/userId/sessionId 为归属边界，cancelledAt 为取消时间。
    -->
    <update id="cancelOpenBySession">
        UPDATE fin_ex_chat_hitl_request_t
        SET status = 'CANCELLED',
            cancelled_at = #{cancelledAt},
            updated_at = #{cancelledAt}
        WHERE tenant_id = #{tenantId}
          AND user_id = #{userId}
          AND session_id = #{sessionId}
          AND status IN ('WAITING', 'RESPONDING')
    </update>

    <!--
      标记等待请求已过期，避免过期后仍被续接到 Runtime。
      入参: tenantId/userId 为归属边界，hitlRequestId 为目标请求。
    -->
    <update id="markExpired">
        UPDATE fin_ex_chat_hitl_request_t
        SET status = 'EXPIRED',
            updated_at = CURRENT_TIMESTAMP
        WHERE tenant_id = #{tenantId}
          AND user_id = #{userId}
          AND id = #{hitlRequestId}
          AND status = 'WAITING'
    </update>
</mapper>
